#procedure crossing

* check the file in reduze/myresults/???.match.info.inc to find crossing rules 

if (match(Sector(F1x23,?a)));
   Multiply replace_(p2,p3,p3,p2);
   id Sector(F1x23,?a)=Sector(F1,?a)*crossx23;
endif;

if (match(Sector(F2x23,?a)));
   Multiply replace_(p2,p3,p3,p2);
   id Sector(F2x23,?a)=Sector(F2,?a)*crossx23;
endif;

if (match(Sector(F3x23,?a)));
   Multiply replace_(p2,p3,p3,p2);
   id Sector(F3x23,?a)=Sector(F3,?a)*crossx23;
endif;

if (match(Sector(F4x23,?a)));
   Multiply replace_(p2,p3,p3,p2);
   id Sector(F4x23,?a)=Sector(F4,?a)*crossx23;
endif;

if (match(Sector(F5x23,?a)));
   Multiply replace_(p2,p3,p3,p2);
   id Sector(F5x23,?a)=Sector(F5,?a)*crossx23;
endif;

if (match(Sector(F6x23,?a)));
   Multiply replace_(p2,p3,p3,p2);
   id Sector(F6x23,?a)=Sector(F6,?a)*crossx23;
endif;

if (match(Sector(F7x23,?a)));
   Multiply replace_(p2,p3,p3,p2);
   id Sector(F7x23,?a)=Sector(F7,?a)*crossx23;
endif;

if (match(Sector(F8x23,?a)));
   Multiply replace_(p2,p3,p3,p2);
   id Sector(F8x23,?a)=Sector(F8,?a)*crossx23;
endif;

if (match(Sector(F9x23,?a)));
   Multiply replace_(p2,p3,p3,p2);
   id Sector(F9x23,?a)=Sector(F9,?a)*crossx23;
endif;

if (match(Sector(F10x23,?a)));
   Multiply replace_(p2,p3,p3,p2);
   id Sector(F10x23,?a)=Sector(F10,?a)*crossx23;
endif;

if (match(Sector(F11x23,?a)));
   Multiply replace_(p2,p3,p3,p2);
   id Sector(F11x23,?a)=Sector(F11,?a)*crossx23;
endif;

if (match(Sector(F12x23,?a)));
   Multiply replace_(p2,p3,p3,p2);
   id Sector(F12x23,?a)=Sector(F12,?a)*crossx23;
endif;

if (match(Sector(F13x23,?a)));
   Multiply replace_(p2,p3,p3,p2);
   id Sector(F13x23,?a)=Sector(F13,?a)*crossx23;
endif;

if (match(Sector(F14x23,?a)));
   Multiply replace_(p2,p3,p3,p2);
   id Sector(F14x23,?a)=Sector(F14,?a)*crossx23;
endif;

if (match(Sector(F15x23,?a)));
   Multiply replace_(p2,p3,p3,p2);
   id Sector(F15x23,?a)=Sector(F15,?a)*crossx23;
endif;

#endprocedure
